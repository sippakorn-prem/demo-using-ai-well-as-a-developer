My API request returns 401 Unauthorized.

The API key is redacted.

Code:

fetch("https://api.example.com/users", {
  headers: {
    Authorization: "Bearer [REDACTED]"
  }
})

Please help me debug possible causes without needing the real token.
