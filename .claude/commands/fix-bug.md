---
description: Fix and verify the shopping cart accumulator bug.
---

# Fix Shopping Cart Bug

Fix `src/demo.js` with the smallest clear change.

Requirements:

- Change only the accumulator logic inside `getCartTotal`.
- Do not edit `src/solution.js`.
- Run `node src/demo.js` after the edit.
- Report the command output and summarize the change in one sentence.
